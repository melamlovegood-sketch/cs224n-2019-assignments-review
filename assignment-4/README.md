# Assignment 4 — Neural Machine Translation

See `A4_COMPLETION_NOTES.md` for implementation details and verification. The locally trained model achieved corpus BLEU 21.550124. Model and optimizer checkpoints are intentionally excluded; the final translations remain in `outputs/`.

